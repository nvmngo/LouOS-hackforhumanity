import React from 'react';
export default function LiveCaseSummary({draft}){const c=draft.client;return <aside className="ai-summary"><div className="ai-draft-label">Draft case</div><h2>Live Case Summary</h2>{(c.name||c.age)&&<section><h3>Client</h3>{c.name&&<strong>{c.name}</strong>}{c.preferredName&&c.preferredName!==c.name&&<p>Prefers {c.preferredName}</p>}{c.age&&<p>{c.age} years old</p>}{c.accommodation&&<p>{c.accommodation}</p>}</section>}{draft.mainSupportNeed&&<section className="ai-main-need"><h3>Main need</h3><strong>{draft.mainSupportNeed}</strong></section>}{draft.problems.length>0&&<section><h3>Current problems</h3>{draft.problems.map(item=><div className="ai-problem" key={item.category}><span className={`ai-priority ${item.priority.toLowerCase()}`}>{item.priority}</span><div><strong>{item.category}</strong><p>{item.description}</p></div></div>)}</section>}{draft.keyInformation.length>0&&<section><h3>Key information</h3><ul>{draft.keyInformation.slice(-7).map((fact,i)=><li key={i}>{fact}</li>)}</ul></section>}<section className="ai-caseworker"><h3>Caseworker</h3><strong>{draft.caseworker.name}</strong><p>{draft.caseworker.role}</p><p>{draft.caseworker.specialisation}</p></section></aside>}