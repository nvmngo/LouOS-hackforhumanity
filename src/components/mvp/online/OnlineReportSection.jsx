import React from 'react';
import OnlineReportField from '@/components/mvp/online/OnlineReportField';
export default function OnlineReportSection({section,values,onChange}){return <section className={`online-section online-section-${section.number||'confirmation'}`}><header><div><div className="online-section-title"><h2>{section.title}</h2>{section.optional&&<span>Optional</span>}</div>{section.note&&<p>{section.note}</p>}</div></header><div className="online-fields">{section.fields.map(field=><OnlineReportField key={field.id} field={field} value={values[field.id]} values={values} onChange={onChange}/>)}</div></section>}
